# Claude Code

Use **[AGENTS.md](./AGENTS.md)** as the canonical repository instructions (build, test, lint, architecture, safety).

Claude-specific additions: none required for this repo. Keep Claude-only settings (permissions, MCP, hooks) in user or local config unless the team agrees to version them under `.claude/`.
